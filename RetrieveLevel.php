
<? PHP
$severname = ""//add server name
$username = ""//add username
$password = ""//add password
$dbname = ""//add database! add database name
$tablename; //this will be sent over via js

  //assign $tablename a value via POST then check if table exists if it does we need to update the svg in the DOM to contain values based
  // on the contents of the table
  
  
  
  ?>
